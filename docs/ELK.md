### ELK
> 如果你连ELK是什么都不知道，就不用接着往下了，你可以用PlumeLog
- Logstash(数据收集、处理和转换工具)
- Kibana(数据可视化工具)
- Elasticsearch(分布式搜索引擎和分析引擎)

### ELK 收集日志的方式
#### 不修改源日志的格式
> 不需要修改源日志，通过grok进行分析过滤，但是在高压力情绪会成为性能瓶颈；
> 如果需要分析大日志时，可能阻塞正常的日志输出，能不用grok过滤就尽量不用。

#### 修改源日志格式
> 按照需要的日志格式输出规则日志，logstash只负责日志的收集和传输,不对日志做任何的过滤清洗。